# ── Supabase（登入／權限 + 植物雲端資料庫）──────────────────────
# 必要：沒有設定這兩個變數，整個 APP 無法登入，任何功能頁都進不去
# （原本只有植物雲端資料庫用到，現在登入/角色權限也一起依賴同一個專案）。
# 在 Vercel Dashboard > Settings > Environment Variables 設定
# 本機開發複製此檔案為 .env.local 並填入實際值
# Project URL / anon public key 在 Supabase Dashboard > Settings > API 取得
# 建表 SQL 見 supabase/schema.sql（植物庫）與 supabase/auth_schema.sql（登入/角色），
# 依序貼到 Supabase SQL Editor 執行一次即可

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ── 系統設定（可選，目前程式尚未讀取，僅供文件用途）─────────────
VITE_APP_NAME=景觀 AI 設計審查顧問 2.0
VITE_APP_VERSION=2.0.0
